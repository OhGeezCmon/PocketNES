# Build tools

See `tools/README.md` for the canonical commands.

## Notes

- Build/repack runs via **MSYS2 MinGW64** so `make` behavior is consistent.
- You’ll need `DEVKITARM` set for the root `Makefile`.

## Script

- `pocketnes_build.py`

